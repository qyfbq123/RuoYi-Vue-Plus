<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aibang.crm.mapper.AbProjectMapper">
    <update id="approval">
        update ab_project
            set origin_name = CONCAT(IFNULL(origin_name, ''), name),
                name = #{officialName},
                status = 1
        where id = #{id}
    </update>
</mapper>
